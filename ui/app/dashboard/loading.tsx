import { AuthSessionScreen } from "@/components/auth/auth-session-screen"

export default function DashboardLoading() {
  return (
    <AuthSessionScreen
      title="Loading your receipts"
      description="Preparing the next workspace view."
    />
  )
}
